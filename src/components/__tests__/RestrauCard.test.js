{/*import { render,screen } from "@testing-library/react"
import Restraucard from "../Restraucard";
import MOCKS from "../Mocks/RestrauMockData.json";
import "@testing-library/jest-dom"

it("should render Restraucard component with props data",()=>{
    render( 
        <Restraucard ResData={MOCKS}/>
    );
    
    const name  = screen.getByText("Hira Sweets");

    expect(name).toBeInTheDocument();
})*/}